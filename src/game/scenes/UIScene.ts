// UI Scene overlay — Phase 3
// Will handle: damage flash, vignette, in-game particle overlays
import Phaser from "phaser";

export class UIScene extends Phaser.Scene {
  constructor() {
    super({ key: "UIScene", active: false });
  }

  create(): void {
    // Placeholder — activated in Phase 3
  }
}
